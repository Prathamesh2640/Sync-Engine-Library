<resources>
    <!-- Compose app: a NoActionBar Material3 theme. Colours come from Compose's
         MaterialTheme in code; this only configures the Activity window. -->
    <style name="Theme.SyncEngine" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
