<resources>
    <!-- DayNight base theme already adapts to dark mode; this override exists only
         so the resource qualifier resolves cleanly. -->
    <style name="Theme.SyncEngine" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
